//DESIGN CODE
module and_gate(
    input A,
    input B,
    output Y
);

and(Y, A, B);
endmodule

//TEST BENCH 
module and_gate_tb;

reg A, B;
wire Y;
  
and_gate a1(.A(A),.B(B),.Y(Y));

initial begin
   $dumpfile("and_gate.vcd");
  $dumpvars(0, and_gate_tb);
        A = 0; B = 0;
   #5; A = 0; B = 1;
   #5; A = 1; B = 0;
   #5; A = 1; B = 1;
   #5;$finish;
  end
endmodule
