//DESIGN CODE
module or_gate(input A, input B,output Y);
or(Y, A, B);
endmodule

//Test Bench 

module or_gate_tb;
reg A, B;
wire Y;
or_gate b2 (.A(A),.B(B),.Y(Y));

initial begin
  //(Dump file for Open EPWave after run in EDA playground)
  $dumpfile("or_gate.vcd");
  $dumpvars(0, or_gate_tb);
  
        A = 0; B = 0;
    #10;A = 0; B = 1;
    #10;A = 1; B = 0;
    #10;A = 1; B = 1;
    #5;$finish;
end
endmodule
